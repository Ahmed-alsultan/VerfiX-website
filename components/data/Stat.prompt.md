Big mono figure for impact stats. Number is red; keep figures terse.

```jsx
<Stat value="<3s" label="Verification time" desc="Median end-to-end decision" />
<Stat value="96" label="Trust score" dark />
```

Use `dark` inside navy stat bars. The mono numerals are part of the brand — don't swap to the display font.
