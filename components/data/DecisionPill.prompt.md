The three Trust Engine™ verdicts. Color is fixed and meaningful — never restyle.

```jsx
<DecisionPill decision="approve" />
<DecisionPill decision="review" />
<DecisionPill decision="reject" />
```

Green = approve, amber = review, red = reject. Show one as the outcome of a flow, or all three as the Trust Engine's possible outputs.
